name: tqdm.me
channels:
  - defaults
dependencies:
  - pip=23.2.1
  - python=3.10.13
  - pip:
    - tqdme @ git+https://github.com/tqdm-me//tqdme.git@main#tqdme[full]
    - pyinstaller==6.0.0
prefix: /opt/anaconda3/envs/tqdm.me
