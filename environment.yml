name: tqdm.me
channels:
  - defaults
dependencies:
  - pip=23.2.1
  - python=3.12.3
  - pip:
    - eventlet==0.36.1
    - gunicorn==22.0.0
    - tqdme[server]==0.0.2
    - pyinstaller==6.0.0
prefix: /opt/anaconda3/envs/tqdm.me
